{
  "name": "shinobi-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && electron dist/main.js",
    "build": "tsc",
    "start": "electron dist/main.js",
    "package": "source .env 2>/dev/null; electron-builder",
    "release": "source .env 2>/dev/null; electron-builder --publish always"
  },
  "devDependencies": {
    "electron": "^35.1.5",
    "electron-builder": "^26.0.12",
    "typescript": "^5"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  }
}
